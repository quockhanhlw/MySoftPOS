<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    NEO-FINTECH PREMIUM STYLES                        -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->

    <!-- Glassmorphism Card -->
    <style name="NeoCard">
        <item name="android:background">@color/card_background</item>
        <item name="android:padding">16dp</item>
        <item name="android:elevation">8dp</item>
    </style>

    <!-- Amount Display (48sp Bold) -->
    <style name="NeoAmountText">
        <item name="android:textSize">48sp</item>
        <item name="android:textColor">@color/neo_text_amount</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:textAlignment">center</item>
    </style>

    <!-- Amount Label -->
    <style name="NeoAmountLabel">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/neo_text_label</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:letterSpacing">0.1</item>
    </style>

    <!-- Keypad Key (Circular) -->
    <style name="NeoKeypadKey" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:background">@color/neo_keypad_bg</item>
        <item name="android:textColor">@color/neo_keypad_text</item>
        <item name="android:textSize">28sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:gravity">center</item>
        <item name="android:minWidth">72dp</item>
        <item name="android:minHeight">72dp</item>
    </style>

    <!-- Keypad Delete/Clear Key -->
    <style name="NeoKeypadKeyIcon" parent="NeoKeypadKey">
        <item name="android:textSize">20sp</item>
        <item name="iconTint">@color/neo_keypad_text</item>
    </style>

    <!-- Payment Button (Gradient) -->
    <style name="NeoPayButton">
        <item name="android:background">@color/primary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">18sp</item>
        <item name="android:fontFamily">sans-serif-bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:paddingHorizontal">32dp</item>
        <item name="android:paddingVertical">16dp</item>
        <item name="android:elevation">8dp</item>
    </style>

    <!-- Status Pill Badge (Base) -->
    <style name="NeoStatusBadge">
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:paddingHorizontal">12dp</item>
        <item name="android:paddingVertical">4dp</item>
    </style>

    <!-- Header Title -->
    <style name="NeoHeaderTitle">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/neo_primary_dark</item>
        <item name="android:fontFamily">sans-serif-bold</item>
    </style>

    <!-- Header Subtitle -->
    <style name="NeoHeaderSubtitle">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/neo_text_label</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    LEGACY KEYPAD STYLES (Compatibility)              -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->

    <style name="KeypadKey" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="KeypadKeyIcon" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">@color/text_primary</item>
        <item name="iconTint">@color/text_primary</item>
    </style>
    
    <style name="KeypadKeySpacer">
        <item name="android:visibility">invisible</item>
        <item name="android:enabled">false</item>
    </style>
    
    <style name="KeypadDone" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/on_primary</item>
        <item name="cornerRadius">8dp</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!--                    NUMPAD CIRCLE BUTTON STYLE                       -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->

    <!-- Circular Numpad Button (Light gray background, dark text) -->
    <style name="NumpadCircleButton" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:layout_width">72dp</item>
        <item name="android:layout_height">72dp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:background">@drawable/bg_numpad_circle</item>
        <item name="android:textColor">@color/neo_primary_dark</item>
        <item name="android:textSize">24sp</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:gravity">center</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:stateListAnimator">@null</item>
    </style>

    <style name="Widget.MySoftPOS.MerchantActionIcon" parent="Widget.MaterialComponents.Button.Icon">
        <item name="android:layout_width">36dp</item>
        <item name="android:layout_height">36dp</item>
        <item name="android:minWidth">0dp</item>
        <item name="android:minHeight">0dp</item>
        <item name="iconPadding">0dp</item>
        <item name="iconGravity">textStart</item>
        <item name="iconSize">18dp</item>
        <item name="cornerRadius">10dp</item>
        <item name="android:insetLeft">0dp</item>
        <item name="android:insetRight">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="rippleColor">@color/merchant_mgmt_ripple</item>
    </style>

    <style name="Widget.MySoftPOS.MerchantActionIcon.Edit" parent="Widget.MySoftPOS.MerchantActionIcon">
        <item name="backgroundTint">@color/merchant_mgmt_action_edit_bg</item>
        <item name="iconTint">@color/merchant_mgmt_action_edit_icon</item>
    </style>

    <style name="Widget.MySoftPOS.MerchantActionIcon.Delete" parent="Widget.MySoftPOS.MerchantActionIcon">
        <item name="backgroundTint">@color/merchant_mgmt_action_delete_bg</item>
        <item name="iconTint">@color/merchant_mgmt_action_delete_icon</item>
    </style>

    <style name="MerchantDialogAnimation">
        <item name="android:windowEnterAnimation">@anim/slide_up_fade_in</item>
        <item name="android:windowExitAnimation">@anim/fade_out</item>
    </style>

</resources>

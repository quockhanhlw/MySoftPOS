<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <!-- Username -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/register_hint_username"
        android:textColor="@color/auth_text_title"
        android:textStyle="bold"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/etUsername"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/bg_auth_input"
        android:paddingHorizontal="16dp"
        android:hint="cooluser123"
        android:textColorHint="@color/auth_text_hint"
        android:textColor="@color/auth_text_title"
        android:inputType="textNoSuggestions" />

    <!-- Password -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/register_hint_password"
        android:textColor="@color/auth_text_title"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/etPassword"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/bg_auth_input"
        android:paddingHorizontal="16dp"
        android:hint="••••••••"
        android:textColorHint="@color/auth_text_hint"
        android:textColor="@color/auth_text_title"
        android:inputType="textPassword" />

    <!-- Confirm Password -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/register_hint_confirm_password"
        android:textColor="@color/auth_text_title"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/etConfirmPassword"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@drawable/bg_auth_input"
        android:paddingHorizontal="16dp"
        android:hint="••••••••"
        android:textColorHint="@color/auth_text_hint"
        android:textColor="@color/auth_text_title"
        android:inputType="textPassword" />

    <!-- Terms Checkbox -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="24dp"
        android:gravity="center_vertical">

        <CheckBox
            android:id="@+id/cbTerms"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:buttonTint="@color/auth_accent_start" />

        <TextView
            android:id="@+id/tvTermsText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/register_terms_text"
            android:textColor="@color/auth_text_body"
            android:textSize="14sp"
            android:layout_marginStart="8dp"/>
    </LinearLayout>

    <!-- Action Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="32dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnBackStep"
            android:layout_width="wrap_content"
            android:layout_height="60dp"
            android:layout_weight="1"
            android:text="Back"
            android:textSize="16sp"
            android:textStyle="bold"
            app:cornerRadius="16dp"
            app:backgroundTint="@android:color/transparent"
            app:strokeColor="@color/auth_accent_start"
            app:strokeWidth="1dp"
            android:textColor="@color/auth_accent_start"
            android:layout_marginEnd="16dp"
            android:stateListAnimator="@null"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnRegister"
            android:layout_width="wrap_content"
            android:layout_height="60dp"
            android:layout_weight="2"
            android:text="@string/register_button"
            android:textSize="16sp"
            android:textStyle="bold"
            app:cornerRadius="16dp"
            app:backgroundTint="@null"
            android:background="@drawable/bg_auth_button"
            android:textColor="@android:color/white" />
            
    </LinearLayout>

</LinearLayout>



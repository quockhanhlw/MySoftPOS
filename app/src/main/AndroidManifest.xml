<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MySoftPOS">

        <activity
            android:name=".WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />

        <activity
            android:name=".MainDashboardActivity"
            android:exported="false"
            android:label="@string/dashboard_title" />

        <activity
            android:name=".PurchaseAmountActivity"
            android:exported="false"
            android:label="Purchase" />

        <activity
            android:name=".PurchaseCardActivity"
            android:exported="false"
            android:label="Card" />

        <activity
            android:name=".IsoMessagePreviewActivity"
            android:exported="false"
            android:label="ISO8583" />

        <activity
            android:name=".HomeTabsActivity"
            android:exported="false"
            android:label="@string/app_name" />

        <activity
            android:name=".PaymentSuccessActivity"
            android:exported="false"
            android:label="Success" />

    </application>

</manifest>